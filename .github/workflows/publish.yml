name: Publish

on:
  workflow_dispatch:
    inputs:
      version_bump:
        description: "Version bump (patch, minor, major) or exact semver (e.g. 1.2.3)"
        required: true
        type: string
        default: patch
      contracts_repo:
        description: "Repo for contracts source (org/repo)"
        required: false
        type: string
        default: berachain/contracts
      contracts_ref:
        description: "Branch/tag/SHA for contracts source"
        required: false
        type: string
        default: main
      staking_pools_repo:
        description: "Repo for staking-pools source (org/repo)"
        required: false
        type: string
        default: berachain/contracts-staking-pools
      staking_pools_ref:
        description: "Branch/tag/SHA for staking-pools source"
        required: false
        type: string
        default: main

jobs:
  publish:
    environment: production
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 0

      - name: Ensure branch is up to date
        run: |
          git fetch origin ${{ github.ref_name }}
          LOCAL=$(git rev-parse HEAD)
          REMOTE=$(git rev-parse origin/${{ github.ref_name }})
          if [ "$LOCAL" != "$REMOTE" ]; then
            echo "::error::Branch ${{ github.ref_name }} is not up to date with remote. Pull and push latest changes first."
            exit 1
          fi

      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v6.0.8
        with:
          version: 9

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: .nvmrc
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
          registry-url: https://registry.npmjs.org

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@c7450ba673e133f5ee30098b3b54f444d3a2ca2d # v1.8.0

      - run: pnpm install --frozen-lockfile

      - name: Cache cloned repos
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: .repos
          key: repos-v2

      - name: Generate ABIs
        run: >-
          pnpm abi:generate
          --repo contracts=${{ inputs.contracts_repo }}
          --repo staking-pools=${{ inputs.staking_pools_repo }}
          --ref contracts=${{ inputs.contracts_ref }}
          --ref staking-pools=${{ inputs.staking_pools_ref }}
        env:
          GITHUB_TOKEN: ${{ secrets.GH_READ_ONLY_PAT }}

      - name: Prune node_modules and yarn caches from cloned repos
        if: always()
        run: find .repos \( -name node_modules -o -name .yarn-cache \) -type d -prune -exec rm -rf {} +

      - run: pnpm test

      - name: Build
        run: pnpm build
        env:
          NODE_OPTIONS: --max-old-space-size=8192

      - name: Generate changelog
        id: changelog
        run: |
          BUMP="${{ inputs.version_bump }}"
          DIST_TAG="latest"
          if [ "${{ inputs.contracts_ref }}" != "main" ] || [ "${{ inputs.staking_pools_ref }}" != "main" ] || [[ "$BUMP" == pre* ]] || [[ "$BUMP" == *-* ]]; then
            DIST_TAG="beta"
          fi
          pnpm changelog --tag "$DIST_TAG" --out=CHANGELOG_BODY.md || true
          if [ -f CHANGELOG_BODY.md ]; then
            echo "has_changelog=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_changelog=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Resolve release type
        id: release
        run: |
          BUMP="${{ inputs.version_bump }}"
          if [ "${{ inputs.contracts_ref }}" != "main" ] || [ "${{ inputs.staking_pools_ref }}" != "main" ] || [[ "$BUMP" == pre* ]] || [[ "$BUMP" == *-* ]]; then
            echo "type=beta" >> "$GITHUB_OUTPUT"
          else
            echo "type=release" >> "$GITHUB_OUTPUT"
          fi

      - name: Compute version
        run: |
          DIST_TAG=$([[ "${{ steps.release.outputs.type }}" == "beta" ]] && echo "beta" || echo "latest")
          VERSION=$(BUMP="${{ inputs.version_bump }}" DIST_TAG="$DIST_TAG" pnpm run --silent version:compute)
          npm pkg set version="$VERSION"
          echo "Version: $VERSION (@$DIST_TAG)"

      - name: Ensure npm supports OIDC
        run: npm install -g npm@latest

      - name: Publish to npm
        run: |
          if [ "${{ steps.release.outputs.type }}" = "beta" ]; then
            pnpm publish --access public --tag beta --no-git-checks --provenance
          else
            pnpm publish --access public --tag latest --no-git-checks --provenance
          fi

      - name: Tag and push
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "VERSION=${VERSION}" >> "$GITHUB_ENV"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "v${VERSION}"
          git push origin HEAD --tags

      - name: Create GitHub release
        run: |
          PRERELEASE_FLAG=""
          if [ "${{ steps.release.outputs.type }}" = "beta" ]; then
            PRERELEASE_FLAG="--prerelease"
          fi
          NOTES_FLAG="--generate-notes"
          if [ "${{ steps.changelog.outputs.has_changelog }}" = "true" ]; then
            NOTES_FLAG="--notes-file CHANGELOG_BODY.md"
          fi
          gh release create "v${VERSION}" \
            --title "v${VERSION}" \
            $NOTES_FLAG \
            $PRERELEASE_FLAG
        env:
          GH_TOKEN: ${{ github.token }}
